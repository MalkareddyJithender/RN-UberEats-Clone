import Colors from '../../constants/colors';

const styles = {
  textInput: {
    backgroundColor: Colors.white,
    borderRadius: 20,
    fontWeight: '400',
    marginTop: 7,
    marginHorizontal: 12,
  },
  textInputContainer: {
    backgroundColor: Colors.white,
    borderRadius: 50,
    flexDirection: 'row',
  },
};

export default styles;
